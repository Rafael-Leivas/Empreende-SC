{
  "name": "empreende-sc",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "install:all": "cd server && npm install && cd ../client && npm install",
    "build": "cd client && npm run build && cd ../server && npx prisma generate && npm run build",
    "start": "cd server && npx prisma migrate deploy && npx prisma db seed && npm run start",
    "seed": "cd server && npx prisma db seed"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
